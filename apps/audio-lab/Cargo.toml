[package]
name = "relay-audio-lab"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Deterministic headless integration harness for the RELAY audio path"

[dependencies]
relay-audio = { version = "=0.1.0", path = "../../crates/relay-audio" }

[dev-dependencies]
serde_json = "=1.0.145"

[lints]
workspace = true
