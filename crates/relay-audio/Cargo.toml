[package]
name = "relay-audio"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Bounded deterministic composition foundation for RELAY audio"

[dependencies]
relay-clock = { version = "=0.1.0", path = "../relay-clock" }
relay-jitter = { version = "=0.1.0", path = "../relay-jitter" }
relay-opus = { version = "=0.1.0", path = "../relay-opus" }
relay-opus-sys = { version = "=0.1.0", path = "../relay-opus-sys" }
relay-resample = { version = "=0.1.0", path = "../relay-resample" }
relay-rt = { version = "=0.1.0", path = "../relay-rt" }

[dev-dependencies]
relay-resample-test-allocator = { version = "=0.0.0", path = "../relay-resample/tests/allocation-counter" }

[lints]
workspace = true
