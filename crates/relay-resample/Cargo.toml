[package]
name = "relay-resample"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
rubato.workspace = true

[lints]
workspace = true

[dev-dependencies]
relay-resample-test-allocator = { version = "=0.0.0", path = "tests/allocation-counter" }
