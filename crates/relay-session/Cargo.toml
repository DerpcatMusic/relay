[package]
name = "relay-session"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
publish = false
description = "Connect, Link, and Stream session engine for RELAY"

[dependencies]
relay-audio = { version = "=0.1.0", path = "../relay-audio" }
relay-domain = { version = "=0.1.0", path = "../relay-domain" }
relay-rt = { version = "=0.1.0", path = "../relay-rt" }
claxon = "0.4"
flacenc = { version = "0.5.1", default-features = false }
sha2 = "0.10"

[lints]
workspace = true
